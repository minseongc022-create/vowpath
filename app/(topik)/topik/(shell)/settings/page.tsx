import { SettingsClient } from "@/topik/components/settings/SettingsClient";

export default function SettingsPage() {
  return (
    <main className="topik-page">
      <SettingsClient />
    </main>
  );
}
