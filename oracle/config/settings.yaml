# Project Oracle — full stack settings
# Risk minimization > return maximization

system:
  name: Project Oracle
  version: "1.1.0"
  timezone: America/New_York

universe:
  symbols:
    - SPY
    - QQQ
    - IWM
    - AAPL
    - MSFT
    - GOOGL
    - AMZN
    - NVDA
    - META
    - JPM
    - XOM
    - GLD
    - TLT

market_data:
  history_days: 365
  benchmark: SPY
  cache_hours: 6

agent_weights:
  market_intelligence: 0.10
  fundamental: 0.20
  technical: 0.20
  quant: 0.20
  sentiment: 0.10
  portfolio_manager: 0.20

risk:
  max_position_pct: 0.15
  max_sector_pct: 0.35
  max_portfolio_volatility: 0.25
  max_drawdown_halt: 0.12
  min_cash_pct: 0.10
  max_correlation_cluster: 0.85
  atr_stop_multiple: 2.0
  veto_confidence_floor: 0.55

decision:
  buy_threshold: 0.55
  add_threshold: 0.45
  reduce_threshold: -0.35
  sell_threshold: -0.55

sizing:
  target_vol: 0.10

reporting:
  output_dir: ./data/reports
  include_agent_details: true

backtest:
  initial_cash: 100000
  commission_bps: 5
  slippage_bps: 5

execution:
  mode: paper
  require_confirm: true
  max_daily_loss_pct: 0.03
